# Container datasource URL override.
# Keep this path container-internal; map host storage via compose, for example:
# ${HOME}/dbconnector/data:/app/data
spring.datasource.url=jdbc:h2:file:/app/data/dbconnector